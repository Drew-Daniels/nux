name: Docs

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - ".github/workflows/docs.yml"

permissions:
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v6
      - run: |
          HUGO_VERSION=0.160.1
          wget -qO- "https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.tar.gz" | sudo tar xz -C /usr/local/bin hugo
          hugo version
      - run: hugo --minify
        working-directory: docs
      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs/public
      - id: deployment
        uses: actions/deploy-pages@v5
